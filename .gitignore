node_modules/
.neohive/
.agent-bridge/
neohive/data/
neohive/projects.json
agent-bridge/projects.json
*.log

# User-specific MCP configs (generated by npx neohive init)
.mcp.json
.cursor/mcp.json
.codex/
.gemini/

# Root-level package files (not part of npm package — npm publishes from neohive/)
/package.json
/package-lock.json

# Development/planning artifacts
/.claude/
/ClaudePlans/
check-ue5-lock.js

# Tests - never upload to git
neohive/test/

# Secrets - NEVER commit (LAN dashboard auth token lives next to dashboard.js in agent-bridge)
agent-bridge/.lan-token
neohive/.lan-token
*.env
.env.*
.cursor/rules/*
